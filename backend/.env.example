# Exemplo de variáveis de ambiente para o backend
# Copie este arquivo para `.env` (NÃO commit) e preencha com valores reais.

# Porta do servidor
PORT=4242

# Ambiente (development|production)
NODE_ENV=development

# Chave secreta do Stripe (server-side) - mantenha esta chave apenas no servidor
STRIPE_SECRET_KEY=sk_test_your_secret_here

# Chave pública do Stripe (publishable) - pode ser injetada no HTML pelo servidor
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_here

# Segredo para geração de JWT (se usar JWT para auth)
JWT_SECRET=change_this_to_a_strong_random_value

# String de conexão do banco (ex: sqlite path ou URL para postgres)
DATABASE_URL=sqlite:./database.sqlite

# Opcional: provider de KMS / vault
KMS_PROVIDER=none
KMS_KEY_ID=
